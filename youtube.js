// JavaScript source code
function start(){
	var htmlComments = document.getElementsByClassName("style-scope jtd-item-section-renderer);
	var objs = [];
	for(var i = 0; i < htmlComments.length; i++){
		if(format(json) > 0.5){
		<span id = "warningMessage">
			"Are you sure you want to display this hateful comment?"
		</span>
		document.body.innerHTML = document.body.innerHTML.replace(htmlComments[i], #warningMessage);
		}else{
			objs.push({
				element: htmlComments[i],
				text: htmlComments[i].getElementsByClassName("style-scope ydt-comment-renderer")[0].textContent
			});
		}
	}
}
